#include<iostream>
#include<string>
#include<algorithm>
using namespace std;

int main(){

    string str = "aoidneiawch";
    //cout<<str;
    //for convert lower case into upper case
    // for(int i=0;i<str.size();i++)
    //     {
    //         if(str[i]>='a' && str[i]<='z')
    //         {
    //             str[i]-=32;
    //         }
    //     }
    // cout<<str;
    // //for lower case
    // for(int i=0;i<str.size();i++)
    // {
    //     if(str[i]>='A' && str[i]<='Z')
    //     {
    //         str[i]+=32;
    //     }
    // }

    // transform(str.begin(), str.end(), str.begin(),::toupper);
    // cout<<endl<<str;
    // transform(str.begin(),str.end(), str.begin(),::tolower);
    // cout<<endl<<str;

    // Short using algorithm header

    // string a = "851290896";
    // sort(a.begin(),a.end(),greater<int>());
    // cout<<a;

    // 

    return 0;
}